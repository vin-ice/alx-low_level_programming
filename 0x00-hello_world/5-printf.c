#include <stdio.h>
/**
 * main - executes printf function from io library
 * Return: returns 0 to standard output
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}

