#include "main.h"
/**
 * _atoi - converts a string to an integer
 * @s: string reference
 * Return: returns 0 if no character matches an integer else 1
 */
int _atoi(char *s)
{
      if (extract_num(*s))
      {
      
      } 
	return (0);
}
int extract_num(*s)
{
	while ()
}
